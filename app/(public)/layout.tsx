"use client";

const MarketingLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      <main className="h-fulll">{children}</main>
    </>
  );
};

export default MarketingLayout;
